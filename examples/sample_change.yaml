image:
  repository: myapp/backend
  tag: v2.3.1   # upgraded from v2.2.0

env:
  - name: LOG_LEVEL
    value: INFO

strategy:
  type: RollingUpdate
  rollingUpdate:
    maxUnavailable: 1
    maxSurge: 1
